# Base source directory 
SOURCE_BASE_DIR="${HOME}/Documents/Programming" # give only the root folder where the projects live

# Base backup directory
BACKUP_BASE_DIR="${HOME}/zBackups" # give the root folder where you want dated backup folders to be created in

# List of project folders to backup, give only the folder names of each Avid project
PROJECTS=("Bash" "Python") # add more projetcs by separating them with spaces
